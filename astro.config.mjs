// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://rebsem.github.io/cursivo-site',
	base: '/cursivo-site',
	output: 'static',
});
